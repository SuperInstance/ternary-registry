[package]
name = "ternary-registry"
version = "0.1.0"
edition = "2021"
description = "Ternary service registry for GPU fleet. Nodes register capabilities, clients discover services. Health-based ternary scoring: healthy(1), degraded(0), failed(-1). CRDT sync."
license = "MIT"
repository = "https://github.com/SuperInstance/ternary-registry"
